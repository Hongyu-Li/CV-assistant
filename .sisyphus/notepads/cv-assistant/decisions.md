## Profile Data Structure

- Chose to use a structured form (JSON-backed) for the Profile data (Personal Info, Work Experience, Projects) instead of raw Markdown, to provide a better UX and easier validation/editing. This aligns with the "preferred" option in the requirements. The data is saved as `profile.json`.
- Future enhancement could involve generating Markdown from this structured data for the CV generation step.
